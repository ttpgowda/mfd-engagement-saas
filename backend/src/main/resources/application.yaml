spring:
  datasource:
    url: jdbc:postgresql://${DATASOURCE_URL}/${DATASOURCE_DATABASE}
    username: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}
  mail:
    host: smtpout.secureserver.net
    port: 587
    username: info@thewealthweb.in
    password: Jx/*UpS4x&VvYL.
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtpout.secureserver.net
            enable: false
            protocols: TLSv1.2
          from: info@thewealthweb.in
# Jwt secret
jwt:
  secret: ${JWT_SECRET}

app:
  domain: localhost # Default to localhost for dev, override with thewealthweb.in for prod